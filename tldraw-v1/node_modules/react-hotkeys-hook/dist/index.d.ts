import { useIsHotkeyPressed } from 'react-hotkeys-hook/dist/useIsHotkeyPressed';
import { useHotkeys, Options } from 'react-hotkeys-hook/dist/useHotkeys';
declare const isHotkeyPressed: {
    (keyCode: number): boolean;
    (keyCode: string): boolean;
};
export { useHotkeys, useIsHotkeyPressed, isHotkeyPressed, Options };
