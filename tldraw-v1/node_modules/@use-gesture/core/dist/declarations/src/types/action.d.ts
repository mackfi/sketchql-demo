import type { ResolverMap } from '@use-gesture/core/dist/declarations/src/config/resolver';
import type { Controller } from '@use-gesture/core/dist/declarations/src/Controller';
import type { Engine } from '@use-gesture/core/dist/declarations/src/engines/Engine';
import { GestureKey } from '@use-gesture/core/dist/declarations/src/types/config';
export declare type EngineClass<Key extends GestureKey> = {
    new (controller: Controller, args: any[], key: Key): Engine<Key>;
};
export declare type Action = {
    key: GestureKey;
    engine: EngineClass<GestureKey>;
    resolver: ResolverMap;
};
