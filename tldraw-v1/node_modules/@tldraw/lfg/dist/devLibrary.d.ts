import { BuildOptions } from 'esbuild';
export declare function devLibrary({ name, isNode, outdir, tsconfig, external, target, define, format, entryPoints, customEsbuildConfig, buildCss, }: {
    name: string;
    outdir: string;
    calculateSize: boolean;
    entryPoints: string[];
    isNode: boolean;
    tsconfig: string;
    external: string[];
    format: 'esm' | 'cjs' | ('esm' | 'cjs')[];
    target: string;
    define: Record<string, string>;
    customEsbuildConfig: BuildOptions;
    buildCss?: {
        entryPoints: string[];
    };
}): Promise<void>;
//# sourceMappingURL=devLibrary.d.ts.map