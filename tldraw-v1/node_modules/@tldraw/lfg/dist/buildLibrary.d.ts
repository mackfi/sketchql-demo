import { BuildOptions } from 'esbuild';
export declare function buildLibrary({ name, isNode, outdir, tsconfig, external, format, target, define, entryPoints, calculateSize, customEsbuildConfig, buildCss, }: {
    name: string;
    outdir: string;
    calculateSize: boolean;
    entryPoints: string[];
    isNode: boolean;
    format: 'esm' | 'cjs' | ('esm' | 'cjs')[];
    target: string;
    tsconfig: string;
    external: string[];
    define: Record<string, string>;
    customEsbuildConfig: BuildOptions;
    buildCss?: {
        entryPoints: string[];
        outfile?: string;
    };
}): Promise<void>;
//# sourceMappingURL=buildLibrary.d.ts.map